# Experiment 016 — low_cpu_mem_usage loading

- **Category:** system
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Streamed weight loading lowers peak RAM during init.

## Rationale (bottleneck targeted)
Model-loading bottleneck + 24GB ceiling.

## Prior expectation
Affects load time/peak RAM, not steady tok/s.

## Implementation
Config delta applied to the best configuration:

```json
{
  "low_cpu_mem_usage": true,
  "label": "low_cpu_mem_load"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": true,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 34.322 |
| decode tok/s | 35.223 |
| mean TTFT (s) | 0.0665 |
| mean latency (s) | 2.4858 |
| peak memory (MB) | 1842.38 |
| CPU util (%) | 78.73 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -6.04%
- vs **current best** (Static KV cache): -18.08%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Affects load time/peak RAM, not steady tok/s.
