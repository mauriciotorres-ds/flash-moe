# Experiment 005 — bfloat16 weights

- **Category:** quantization
- **Applied on:** baseline config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
bf16 keeps fp32 dynamic range with fp16 bandwidth.

## Rationale (bottleneck targeted)
Compare against fp16 for a quality/throughput trade.

## Prior expectation
Throughput similar to fp16; possibly better numerics.

## Implementation
Config delta applied to the baseline configuration:

```json
{
  "dtype": "bfloat16",
  "label": "bf16"
}
```

Resulting engine signature:

```json
{
  "dtype": "bfloat16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 33.069 |
| decode tok/s | 34.115 |
| mean TTFT (s) | 0.0801 |
| mean latency (s) | 2.4228 |
| peak memory (MB) | 2066.01 |
| CPU util (%) | 92.92 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.685 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -9.47%
- vs **current best** (baseline): -9.47%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Throughput similar to fp16; possibly better numerics.
