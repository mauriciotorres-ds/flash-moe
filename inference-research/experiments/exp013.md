# Experiment 013 — torch.compile (default)

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Graph capture fuses ops and cuts dispatch overhead.

## Rationale (bottleneck targeted)
Per-token dispatch overhead is large for small models.

## Prior expectation
First-call compile cost; warmup hides it.

## Implementation
Config delta applied to the best configuration:

```json
{
  "torch_compile": true,
  "compile_mode": "default",
  "label": "compile_default"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": true,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 36.177 |
| decode tok/s | 37.133 |
| mean TTFT (s) | 0.0639 |
| mean latency (s) | 2.3442 |
| peak memory (MB) | 1931.56 |
| CPU util (%) | 78.33 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -0.96%
- vs **current best** (Static KV cache): -13.65%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
First-call compile cost; warmup hides it.
