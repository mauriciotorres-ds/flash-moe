# Experiment 007 — FlashAttention-2

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
FA2 reduces memory movement in attention.

## Rationale (bottleneck targeted)
Test whether FA2 kernels are usable on this host.

## Prior expectation
Expected UNSUPPORTED on MPS -> graceful fallback (a failure to log).

## Implementation
Config delta applied to the best configuration:

```json
{
  "attn_implementation": "flash_attention_2",
  "label": "flash_attn2"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "flash_attention_2",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 39.179 |
| decode tok/s | 40.146 |
| mean TTFT (s) | 0.0574 |
| mean latency (s) | 2.1589 |
| peak memory (MB) | 2090.43 |
| CPU util (%) | 52.88 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +7.26%
- vs **current best** (sdpa): -1.56%

## Support Notes
  - load fallback (dropped optional kwargs): FlashAttention2 has been toggled on, but it cannot be used due to the following error: the package for FlashAttention2 doesn't seem to be installed.

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Expected UNSUPPORTED on MPS -> graceful fallback (a failure to log).
