# Experiment 002 — torch.inference_mode() context

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
inference_mode is stricter than no_grad and skips version counters.

## Rationale (bottleneck targeted)
Targets per-token Python/ATen overhead, the dominant cost at 0.5B.

## Prior expectation
Usually >= no_grad; expected to replace it.

## Implementation
Config delta applied to the best configuration:

```json
{
  "inference_mode": true,
  "no_grad": false,
  "label": "inference_mode"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": true,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 36.185 |
| decode tok/s | 37.177 |
| mean TTFT (s) | 0.0649 |
| mean latency (s) | 2.321 |
| peak memory (MB) | 1549.5 |
| CPU util (%) | 78.64 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -0.94%
- vs **current best** (baseline): -0.94%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Usually >= no_grad; expected to replace it.
