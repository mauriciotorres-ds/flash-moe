# Experiment 022 — Prompt batching (bs=4)

- **Category:** scheduling
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Batching amortises kernel launches across prompts.

## Rationale (bottleneck targeted)
Throughput scheduling: improves aggregate tok/s under load.

## Prior expectation
Improves throughput, not single-stream latency.

## Implementation
Config delta applied to the best configuration:

```json
{
  "batch_size": 4,
  "label": "batch4"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 4,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 27.975 |
| decode tok/s | 28.66 |
| mean TTFT (s) | 0.0827 |
| mean latency (s) | 3.0324 |
| peak memory (MB) | 1838.81 |
| CPU util (%) | 84.3 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -23.41%
- vs **current best** (Static KV cache): -33.23%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Improves throughput, not single-stream latency.
