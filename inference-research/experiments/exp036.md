# Experiment 036 — float32 matmul precision = high (TF32-style)

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Allow reduced-precision fp32 matmul for throughput.

## Rationale (bottleneck targeted)
Compute bottleneck on the GPU matmuls.

## Prior expectation
Backend-dependent; measure for MPS.

## Implementation
Config delta applied to the best configuration:

```json
{
  "matmul_precision": "high",
  "label": "matmul_high"
}
```

Resulting engine signature:

```json
{
  "dtype": "float16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 51.092 |
| decode tok/s | 52.713 |
| mean TTFT (s) | 0.0518 |
| mean latency (s) | 1.3861 |
| peak memory (MB) | 2046.56 |
| CPU util (%) | 85.49 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.543 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +39.87%
- vs **current best** (adaptive_maxtok): -5.67%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Backend-dependent; measure for MPS.
