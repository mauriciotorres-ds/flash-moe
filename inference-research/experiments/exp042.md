# Experiment 042 — Final stacked best-of-all

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Apply every validated win together as the optimized engine candidate.

## Rationale (bottleneck targeted)
Produce the final optimized configuration.

## Prior expectation
Should equal or beat every individual experiment.

## Implementation
Config delta applied to the best configuration:

```json
{
  "label": "final_stacked"
}
```

Resulting engine signature:

```json
{
  "dtype": "float16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 4,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 54.75 |
| decode tok/s | 56.632 |
| mean TTFT (s) | 0.0498 |
| mean latency (s) | 1.2801 |
| peak memory (MB) | 1603.26 |
| CPU util (%) | 84.59 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.543 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +49.89%
- vs **current best** (dynamic_batch): -3.29%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Should equal or beat every individual experiment.
