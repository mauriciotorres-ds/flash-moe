# Experiment 030 — SDPA + fp16 combo

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **KEEP**

## Hypothesis
Fused attention on half precision compounds two wins.

## Rationale (bottleneck targeted)
Attack compute and bandwidth bottlenecks together.

## Prior expectation
Likely additive.

## Implementation
Config delta applied to the best configuration:

```json
{
  "attn_implementation": "sdpa",
  "dtype": "float16",
  "label": "sdpa+fp16"
}
```

Resulting engine signature:

```json
{
  "dtype": "float16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 53.75 |
| decode tok/s | 55.613 |
| mean TTFT (s) | 0.0495 |
| mean latency (s) | 1.5539 |
| peak memory (MB) | 2244.67 |
| CPU util (%) | 85.81 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.706 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +47.15%
- vs **current best** (Static KV cache): +28.29%

## Support Notes
  - none

## Decision
**KEEP** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Likely additive.
