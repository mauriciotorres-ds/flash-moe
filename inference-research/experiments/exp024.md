# Experiment 024 — Speculative decoding (draft model)

- **Category:** decoding
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
A small draft proposes tokens the target verifies in parallel.

## Rationale (bottleneck targeted)
Decode is latency-bound; speculation can raise effective tok/s.

## Prior expectation
Gains depend on draft acceptance rate; can break even.

## Implementation
Config delta applied to the best configuration:

```json
{
  "speculative": true,
  "assistant_model_id": "Qwen/Qwen2.5-0.5B-Instruct",
  "num_assistant_tokens": 5,
  "label": "speculative"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": true,
  "assistant_model_id": "Qwen/Qwen2.5-0.5B-Instruct"
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 6.199 |
| decode tok/s | 7.083 |
| mean TTFT (s) | 0.3157 |
| mean latency (s) | 2.5477 |
| peak memory (MB) | 1448.28 |
| CPU util (%) | 59.77 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.656 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -83.03%
- vs **current best** (Static KV cache): -85.20%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Gains depend on draft acceptance rate; can break even.
