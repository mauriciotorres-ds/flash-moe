# Experiment 028 — Pinned host memory

- **Category:** memory
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Pinned memory speeds host<->device transfers.

## Rationale (bottleneck targeted)
Targets H2D copy cost in the input pipeline.

## Prior expectation
Mainly a CUDA win; near-neutral on unified-memory MPS.

## Implementation
Config delta applied to the best configuration:

```json
{
  "pin_memory": true,
  "label": "pinned_mem"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": true,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 35.262 |
| decode tok/s | 36.175 |
| mean TTFT (s) | 0.0657 |
| mean latency (s) | 2.4167 |
| peak memory (MB) | 1521.81 |
| CPU util (%) | 74.33 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -3.46%
- vs **current best** (Static KV cache): -15.84%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Mainly a CUDA win; near-neutral on unified-memory MPS.
