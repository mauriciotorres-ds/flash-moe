# Experiment 037 — Compile + SDPA + bf16 stack

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Stack the three most promising runtime/precision wins.

## Rationale (bottleneck targeted)
Compound optimization once individual winners are known.

## Prior expectation
Either the new best or reveals a bad interaction.

## Implementation
Config delta applied to the best configuration:

```json
{
  "torch_compile": true,
  "compile_mode": "reduce-overhead",
  "attn_implementation": "sdpa",
  "dtype": "bfloat16",
  "label": "compile+sdpa+bf16"
}
```

Resulting engine signature:

```json
{
  "dtype": "bfloat16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": true,
  "compile_mode": "reduce-overhead",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 52.823 |
| decode tok/s | 54.337 |
| mean TTFT (s) | 0.0446 |
| mean latency (s) | 1.2958 |
| peak memory (MB) | 1077.3 |
| CPU util (%) | 82.69 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.513 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +44.61%
- vs **current best** (adaptive_maxtok): -2.48%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Either the new best or reveals a bad interaction.
