# Experiment 006 — SDPA attention

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **KEEP**

## Hypothesis
Fused scaled-dot-product-attention beats eager Python attention.

## Rationale (bottleneck targeted)
Attention is a per-layer cost; eager is the naive reference.

## Prior expectation
Reliable win where supported.

## Implementation
Config delta applied to the best configuration:

```json
{
  "attn_implementation": "sdpa",
  "label": "sdpa"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 39.8 |
| decode tok/s | 40.752 |
| mean TTFT (s) | 0.0551 |
| mean latency (s) | 2.1231 |
| peak memory (MB) | 1945.08 |
| CPU util (%) | 53.57 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +8.96%
- vs **current best** (baseline): +8.96%

## Support Notes
  - none

## Decision
**KEEP** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Reliable win where supported.
