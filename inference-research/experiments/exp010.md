# Experiment 010 — use_cache ablation

- **Category:** memory
- **Applied on:** baseline config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Disabling the KV cache forces full recompute each step.

## Rationale (bottleneck targeted)
Ablation: quantify how much the KV cache is worth.

## Prior expectation
Expected MUCH slower; validates cache value.

## Implementation
Config delta applied to the baseline configuration:

```json
{
  "use_cache": false,
  "label": "no_kv_cache(ablation)"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": false,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 18.232 |
| decode tok/s | 18.491 |
| mean TTFT (s) | 0.086 |
| mean latency (s) | 5.4203 |
| peak memory (MB) | 1368.13 |
| CPU util (%) | 55.26 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -50.09%
- vs **current best** (sdpa): -54.19%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Expected MUCH slower; validates cache value.
