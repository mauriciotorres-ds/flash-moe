# Experiment 019 — Slow (Python) tokenizer ablation

- **Category:** runtime
- **Applied on:** baseline config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
The Rust fast tokenizer should beat the Python one at encode.

## Rationale (bottleneck targeted)
Tokenizer/startup bottleneck (cf. Flash-MoE's C BPE, 20x startup).

## Prior expectation
Fast tokenizer expected better; this run should regress.

## Implementation
Config delta applied to the baseline configuration:

```json
{
  "use_fast_tokenizer": false,
  "label": "slow_tokenizer(ablation)"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": false,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 27.996 |
| decode tok/s | 28.689 |
| mean TTFT (s) | 0.0846 |
| mean latency (s) | 2.9594 |
| peak memory (MB) | 1735.98 |
| CPU util (%) | 82.14 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -23.36%
- vs **current best** (Static KV cache): -33.18%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Fast tokenizer expected better; this run should regress.
