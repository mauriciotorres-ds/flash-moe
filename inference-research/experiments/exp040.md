# Experiment 040 — Batch tokenizer encode

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **KEEP**

## Hypothesis
Encode all prompts in one tokenizer call.

## Rationale (bottleneck targeted)
Tokenizer overhead at the suite level.

## Prior expectation
Helps suite wall-time, not per-token decode.

## Implementation
Config delta applied to the best configuration:

```json
{
  "batch_tokenize": true,
  "label": "batch_tokenize"
}
```

Resulting engine signature:

```json
{
  "dtype": "float16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 55.537 |
| decode tok/s | 57.569 |
| mean TTFT (s) | 0.0508 |
| mean latency (s) | 1.2633 |
| peak memory (MB) | 1397.1 |
| CPU util (%) | 89.82 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.543 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +52.04%
- vs **current best** (gen_config_cache): +4.30%

## Support Notes
  - none

## Decision
**KEEP** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Helps suite wall-time, not per-token decode.
