# Experiment 014 — torch.compile (reduce-overhead)

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
CUDA-graph-style overhead reduction for the decode loop.

## Rationale (bottleneck targeted)
Decode loop is launch-bound; reduce-overhead targets exactly that.

## Prior expectation
Often best compile mode for autoregressive decode.

## Implementation
Config delta applied to the best configuration:

```json
{
  "torch_compile": true,
  "compile_mode": "reduce-overhead",
  "label": "compile_reduce_ovh"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": true,
  "compile_mode": "reduce-overhead",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 35.547 |
| decode tok/s | 36.489 |
| mean TTFT (s) | 0.0639 |
| mean latency (s) | 2.3971 |
| peak memory (MB) | 1851.65 |
| CPU util (%) | 78.59 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -2.68%
- vs **current best** (Static KV cache): -15.16%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Often best compile mode for autoregressive decode.
