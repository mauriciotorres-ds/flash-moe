# Experiment 034 — Attention slicing

- **Category:** memory
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Compute attention in chunks to cap peak memory.

## Rationale (bottleneck targeted)
Lets longer contexts fit under the 24GB ceiling.

## Prior expectation
Trades a little speed for headroom.

## Implementation
Config delta applied to the best configuration:

```json
{
  "attention_slicing": true,
  "label": "attn_slicing"
}
```

Resulting engine signature:

```json
{
  "dtype": "float16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 53.926 |
| decode tok/s | 55.712 |
| mean TTFT (s) | 0.0489 |
| mean latency (s) | 1.5454 |
| peak memory (MB) | 2262.73 |
| CPU util (%) | 87.13 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.706 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +47.63%
- vs **current best** (sdpa+fp16): +0.33%

## Support Notes
  - attention_slicing not exposed by this model; use sdpa mem_efficient backend (exp9) for the same goal.

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Trades a little speed for headroom.
