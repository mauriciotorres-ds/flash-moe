# Experiment 035 — Length-adaptive max tokens

- **Category:** decoding
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **KEEP**

## Hypothesis
Cap generation per category to avoid runaway decode.

## Rationale (bottleneck targeted)
Token-generation bottleneck; stop spending on low-value tokens.

## Prior expectation
Latency win; must not truncate needed output (quality check).

## Implementation
Config delta applied to the best configuration:

```json
{
  "max_tokens_scale": 0.75,
  "label": "adaptive_maxtok"
}
```

Resulting engine signature:

```json
{
  "dtype": "float16",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 54.164 |
| decode tok/s | 56.127 |
| mean TTFT (s) | 0.0509 |
| mean latency (s) | 1.2925 |
| peak memory (MB) | 2061.98 |
| CPU util (%) | 90.79 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 1.543 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +48.28%
- vs **current best** (sdpa+fp16): +0.77%

## Support Notes
  - none

## Decision
**KEEP** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Latency win; must not truncate needed output (quality check).
