# Experiment 018 — Pin threads to performance cores

- **Category:** system
- **Applied on:** baseline config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
On CPU paths, limiting to the 4 P-cores avoids E-core contention.

## Rationale (bottleneck targeted)
System scheduling bottleneck on the M4's hybrid CPU.

## Prior expectation
Only relevant if a CPU fallback path is used.

## Implementation
Config delta applied to the baseline configuration:

```json
{
  "num_threads": 4,
  "device": "cpu",
  "label": "cpu_4threads_pcores"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "cpu",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": 4,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 33.792 |
| decode tok/s | 35.51 |
| mean TTFT (s) | 0.1056 |
| mean latency (s) | 2.4856 |
| peak memory (MB) | 3458.81 |
| CPU util (%) | 141.82 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | cpu |

## Performance Change
- vs **baseline**: -7.49%
- vs **current best** (Static KV cache): -19.35%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Only relevant if a CPU fallback path is used.
