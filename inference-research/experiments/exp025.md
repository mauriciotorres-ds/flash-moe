# Experiment 025 — Greedy vs sampling cost

- **Category:** decoding
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Greedy avoids sampling/softmax-top-k overhead.

## Rationale (bottleneck targeted)
Isolate sampler cost from model compute.

## Prior expectation
Greedy slightly faster; default decode for benchmarking.

## Implementation
Config delta applied to the best configuration:

```json
{
  "do_sample": false,
  "label": "greedy_decode"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 35.096 |
| decode tok/s | 36.048 |
| mean TTFT (s) | 0.0677 |
| mean latency (s) | 2.4238 |
| peak memory (MB) | 2264.53 |
| CPU util (%) | 78.47 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -3.92%
- vs **current best** (Static KV cache): -16.23%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Greedy slightly faster; default decode for benchmarking.
