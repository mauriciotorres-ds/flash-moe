# Experiment 020 — INT8 (bitsandbytes)

- **Category:** quantization
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
8-bit weights cut memory traffic ~4x vs fp32.

## Rationale (bottleneck targeted)
Bandwidth bottleneck; biggest theoretical memory win.

## Prior expectation
bitsandbytes is CUDA-only -> expected UNSUPPORTED on MPS.

## Implementation
Config delta applied to the best configuration:

```json
{
  "quantization": "int8",
  "quant_backend": "bitsandbytes",
  "label": "int8_bnb"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "int8",
  "quant_backend": "bitsandbytes",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 27.205 |
| decode tok/s | 27.993 |
| mean TTFT (s) | 0.0906 |
| mean latency (s) | 3.6509 |
| peak memory (MB) | 1743.57 |
| CPU util (%) | 81.37 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -25.52%
- vs **current best** (Static KV cache): -35.07%

## Support Notes
  - quantization=int8 via bitsandbytes requires CUDA; not supported on mps. Running unquantized.

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
bitsandbytes is CUDA-only -> expected UNSUPPORTED on MPS.
