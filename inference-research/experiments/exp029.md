# Experiment 029 — Static cache + max-autotune compile

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Static cache gives compile a fixed-shape graph -> best fusion.

## Rationale (bottleneck targeted)
Combine two prior wins; classic fast-decode recipe.

## Prior expectation
Expected strong combo if both individually helped.

## Implementation
Config delta applied to the best configuration:

```json
{
  "cache_implementation": "static",
  "torch_compile": true,
  "compile_mode": "max-autotune",
  "label": "static+autotune"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": true,
  "compile_mode": "max-autotune",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "static",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 38.606 |
| decode tok/s | 39.598 |
| mean TTFT (s) | 0.0623 |
| mean latency (s) | 2.1865 |
| peak memory (MB) | 2532.9 |
| CPU util (%) | 76.96 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +5.69%
- vs **current best** (Static KV cache): -7.86%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Expected strong combo if both individually helped.
