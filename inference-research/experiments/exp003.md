# Experiment 003 — model.eval() isolation

- **Category:** runtime
- **Applied on:** baseline config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Leaving train mode on enables dropout/other train-time paths.

## Rationale (bottleneck targeted)
Ablation to confirm eval() matters and the harness detects it.

## Prior expectation
eval() expected better; this run should be slower/worse quality.

## Implementation
Config delta applied to the baseline configuration:

```json
{
  "eval_mode": false,
  "label": "no_eval(ablation)"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": false,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 34.528 |
| decode tok/s | 35.421 |
| mean TTFT (s) | 0.0667 |
| mean latency (s) | 2.4353 |
| peak memory (MB) | 1894.92 |
| CPU util (%) | 77.84 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -5.47%
- vs **current best** (baseline): -5.47%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
eval() expected better; this run should be slower/worse quality.
