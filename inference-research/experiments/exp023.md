# Experiment 023 — Prompt batching (bs=8)

- **Category:** scheduling
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Larger batch -> higher utilisation until memory-bound.

## Rationale (bottleneck targeted)
Find the batch size that saturates the 10-core GPU.

## Prior expectation
Diminishing returns / memory pressure past some point.

## Implementation
Config delta applied to the best configuration:

```json
{
  "batch_size": 8,
  "label": "batch8"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 8,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 26.411 |
| decode tok/s | 27.013 |
| mean TTFT (s) | 0.0923 |
| mean latency (s) | 4.2216 |
| peak memory (MB) | 1855.85 |
| CPU util (%) | 83.67 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -27.69%
- vs **current best** (Static KV cache): -36.96%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Diminishing returns / memory pressure past some point.
