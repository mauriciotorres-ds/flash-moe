# Experiment 017 — channels_last memory format

- **Category:** memory
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
channels_last can improve some kernel memory access.

## Rationale (bottleneck targeted)
Cheap to try; targets memory layout.

## Prior expectation
Usually neutral for transformers; measure.

## Implementation
Config delta applied to the best configuration:

```json
{
  "channels_last": true,
  "label": "channels_last"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": true,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 34.517 |
| decode tok/s | 35.407 |
| mean TTFT (s) | 0.0661 |
| mean latency (s) | 2.4455 |
| peak memory (MB) | 1517.88 |
| CPU util (%) | 78.95 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -5.50%
- vs **current best** (Static KV cache): -17.62%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Usually neutral for transformers; measure.
