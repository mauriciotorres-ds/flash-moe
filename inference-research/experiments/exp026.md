# Experiment 026 — Early EOS stopping

- **Category:** decoding
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Stop as soon as EOS is produced instead of padding to max.

## Rationale (bottleneck targeted)
Token-generation bottleneck: don't generate wasted tokens.

## Prior expectation
Lowers latency on prompts that finish early.

## Implementation
Config delta applied to the best configuration:

```json
{
  "early_stop_eos": true,
  "label": "early_eos"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 34.734 |
| decode tok/s | 35.665 |
| mean TTFT (s) | 0.0657 |
| mean latency (s) | 2.4775 |
| peak memory (MB) | 1747.81 |
| CPU util (%) | 75.39 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -4.91%
- vs **current best** (Static KV cache): -17.10%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Lowers latency on prompts that finish early.
