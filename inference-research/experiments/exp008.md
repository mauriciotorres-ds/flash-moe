# Experiment 008 — SDPA math backend

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Forcing the math kernel can be faster for tiny head dims.

## Rationale (bottleneck targeted)
Probe which SDPA backend the scheduler should pin.

## Prior expectation
Backend-dependent; measure.

## Implementation
Config delta applied to the best configuration:

```json
{
  "sdpa_backend": "math",
  "label": "sdpa_math"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "sdpa",
  "sdpa_backend": "math",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 38.996 |
| decode tok/s | 39.917 |
| mean TTFT (s) | 0.0562 |
| mean latency (s) | 2.1786 |
| peak memory (MB) | 1614.64 |
| CPU util (%) | 53.09 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: +6.76%
- vs **current best** (sdpa): -2.02%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Backend-dependent; measure.
