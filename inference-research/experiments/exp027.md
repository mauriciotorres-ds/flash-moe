# Experiment 027 — Persistent engine reuse (no reload)

- **Category:** system
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Keeping the model resident + extra warmup yields steady-state tok/s.

## Rationale (bottleneck targeted)
Model-loading/warmup overhead removed from the hot path.

## Prior expectation
Improves measured steady-state; standard serving practice.

## Implementation
Config delta applied to the best configuration:

```json
{
  "warmup_runs": 2,
  "label": "warm_persistent"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 33.866 |
| decode tok/s | 34.713 |
| mean TTFT (s) | 0.0657 |
| mean latency (s) | 2.4919 |
| peak memory (MB) | 1564.87 |
| CPU util (%) | 77.7 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -7.29%
- vs **current best** (Static KV cache): -19.17%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Improves measured steady-state; standard serving practice.
