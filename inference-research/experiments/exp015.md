# Experiment 015 — torch.compile (max-autotune)

- **Category:** runtime
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
Autotuned kernels maximise throughput.

## Rationale (bottleneck targeted)
Spend compile time to win steady-state tok/s.

## Prior expectation
Long compile; may or may not beat reduce-overhead.

## Implementation
Config delta applied to the best configuration:

```json
{
  "torch_compile": true,
  "compile_mode": "max-autotune",
  "label": "compile_max_autotune"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": true,
  "compile_mode": "max-autotune",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "none",
  "quant_backend": "none",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 34.885 |
| decode tok/s | 35.766 |
| mean TTFT (s) | 0.0688 |
| mean latency (s) | 2.3715 |
| peak memory (MB) | 1431.14 |
| CPU util (%) | 78.01 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -4.50%
- vs **current best** (Static KV cache): -16.74%

## Support Notes
  - none

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
Long compile; may or may not beat reduce-overhead.
