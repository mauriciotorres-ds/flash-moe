# Experiment 021 — NF4 4-bit (bitsandbytes)

- **Category:** quantization
- **Applied on:** best config
- **Data source:** MEASURED (`measured=true`)
- **Decision:** **DISCARD**

## Hypothesis
4-bit weights for maximum memory reduction.

## Rationale (bottleneck targeted)
Mirrors Flash-MoE's 4-bit expert packing.

## Prior expectation
CUDA-only; expected UNSUPPORTED on MPS. Quality risk if forced.

## Implementation
Config delta applied to the best configuration:

```json
{
  "quantization": "nf4",
  "quant_backend": "bitsandbytes",
  "label": "nf4_bnb"
}
```

Resulting engine signature:

```json
{
  "dtype": "float32",
  "device": "mps",
  "inference_mode": false,
  "no_grad": false,
  "eval_mode": true,
  "torch_compile": false,
  "compile_mode": "default",
  "num_threads": null,
  "channels_last": false,
  "attn_implementation": "eager",
  "sdpa_backend": "auto",
  "use_cache": true,
  "cache_implementation": "dynamic",
  "low_cpu_mem_usage": false,
  "pin_memory": false,
  "quantization": "nf4",
  "quant_backend": "bitsandbytes",
  "use_fast_tokenizer": true,
  "do_sample": false,
  "batch_size": 1,
  "speculative": false,
  "assistant_model_id": null
}
```

## Files Modified
- `ireng/experiments.py` (registry entry)
- engine behaviour driven by `ireng/engine.py` (config-driven; no code fork)

## Benchmark Results
| metric | value |
|---|---|
| mean tok/s | 28.238 |
| decode tok/s | 28.907 |
| mean TTFT (s) | 0.0805 |
| mean latency (s) | 3.0281 |
| peak memory (MB) | 1822.64 |
| CPU util (%) | 83.53 |
| GPU util (%) | n/a (MPS) |
| KV cache (MB) | 3.411 |
| quality score | 1.0 |
| device | mps |

## Performance Change
- vs **baseline**: -22.69%
- vs **current best** (Static KV cache): -32.60%

## Support Notes
  - quantization=nf4 via bitsandbytes requires CUDA; not supported on mps. Running unquantized.

## Decision
**DISCARD** — kept only if it improved throughput/latency/memory
without degrading output quality (quality floor 0.7).

## Lessons Learned
CUDA-only; expected UNSUPPORTED on MPS. Quality risk if forced.
