# Inference Research Project — Python dependencies
# Target: Apple M4 / 24 GB, macOS, Python 3.11+
#
# Install:
#   pip install -r requirements.txt
#
# llama-cpp-python MUST be installed with Metal support separately:
#   CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python --no-cache-dir

# Core inference (GGUF + Metal)
# llama-cpp-python  <- install separately with Metal flags (see above)

# Metrics / profiling
psutil>=5.9.0

# Benchmark + analysis
numpy>=1.26.0
pandas>=2.1.0

# Plots
matplotlib>=3.8.0

# Dashboard
streamlit>=1.36.0

# Model download
huggingface_hub>=0.23.0

# Kept for report generation (optional)
safetensors>=0.4.0
